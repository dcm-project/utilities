# Compose override for E2E testing: publishes the container SP port to the host.
# Injected automatically by deploy-dcm.sh via the provider registry
# (providers/k8s-container.conf → COMPOSE_OVERRIDE).

services:
  k8s-container-service-provider:
    ports:
      - "8082:8080"
