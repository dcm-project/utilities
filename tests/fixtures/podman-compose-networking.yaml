services:
  database:
    image: docker.io/library/busybox:1.36
    command:
      - sh
      - -c
      - mkdir -p /www && echo database > /www/index.html && exec httpd -f -p 8080 -h /www

  probe:
    image: docker.io/library/busybox:1.36
    command:
      - sh
      - -c
      - sleep 300
